{
  "name": "@go-ai/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "biome check src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
