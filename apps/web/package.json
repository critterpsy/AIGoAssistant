{
  "name": "@go-ai/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "lint": "biome check src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
